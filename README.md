# FactoryRobotHazardAnalyzer

A console-based Java application that evaluates robot hazard risk
based on arm precision, worker density, and machinery state.
Implemented using OOPS principles, custom exception handling,
and GitFlow (UC-wise development).
